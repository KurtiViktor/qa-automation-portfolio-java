/**
 * Пакет, предназначенный для работы с классами расширений и кастомизации, используемых библиотек.
 * Если класс не попадает в пакеты api, web, db, то он попадает сюда.
 * Пакет содержит резолверы Junit 5.
 */
package test_engine.ext.junit5.resolver;